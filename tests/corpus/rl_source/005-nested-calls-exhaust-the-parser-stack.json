{
  "found_by": "tests/fuzz_rl_source.py via tests/test_rl_source_differential.py",
  "id": "005-nested-calls-exhaust-the-parser-stack",
  "kind": "front-end-crash",
  "note": "The same stack exhaustion as 002 reached through a different production, kept separate because a fix that adds a depth counter to _bin alone would leave this one open. 82 nested user-function calls; the inliner never runs, because the parser does not survive to hand it a tree.",
  "signature": "RecursionError: maximum recursion depth exceeded",
  "source_repeat": {
    "after": ";",
    "after_unit": ")",
    "before": "fn f(x) { return x + 1; } input a; output ",
    "count": 82,
    "middle": "a",
    "unit": "f("
  }
}
